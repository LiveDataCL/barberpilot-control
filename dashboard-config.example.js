// Dashboard configuration — EXAMPLE FILE
// Copy this file to dashboard-config.js and fill in the token.
// dashboard-config.js is gitignored and must NEVER be committed.
//
// The token is set in Railway environment variables (DASHBOARD_READ_TOKEN).
// Contact the system administrator to obtain the current token value.
window.DASHBOARD_CONFIG = {
  apiUrl: 'https://barberpilot-api-production.up.railway.app',
  token: ''  // Set via environment — contact admin
};
